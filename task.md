请为 `llspace exporter` 工具新增 **聊天记录导出功能**，具体要求如下：

### 总体架构
- 在 `src/gui.py` 中重构主界面为 **主页（Home Page）**，用户成功登录后进入该主页。
- 主页提供两个操作入口按钮：
  - **“导出卡包”**：进入原有卡包导出流程。
  - **“导出聊天记录”**：进入新增的聊天记录导出流程。
- 任一导出操作（卡包或聊天）完成后，自动返回主页。

---

### 聊天记录导出流程

#### 1. 聊天对话列表页面
- 点击“导出聊天记录”后，跳转到新页面，显示用户的所有聊天对话。
- **数据来源**：调用 `API_specs.md` 中 **“获取会话列表”** 接口。
- **缓存机制**：
  - 首次加载后，将完整响应数据缓存至本地（如 `./cache/conversations.json`），避免重复请求。
  - 页面提供 **“刷新”按钮**，点击后重新调用 API 并更新缓存与界面。
- **列表展示**：
  - 每个对话显示：对话标题（优先使用对方昵称，若无则用 ID）、最后活跃时间、未读状态（如 API 提供）。
  - 支持多选（含“全选/取消全选”复选框）。
  - 提供“选择导出路径”按钮，调用系统文件对话框设定导出根目录。

#### 2. 导出所选对话
- 用户点击“导出选中对话”后，对每个选中的对话执行以下操作：
  - 调用 `API_specs.md` 中 **“获取会话内容”** 接口，获取全部消息记录。

#### 3. 三种格式导出（每个对话独立生成）
对每个对话，生成以下三种文件，保存在 `{导出路径}/{对话名}/` 目录下：

##### (a) Markdown 文件（`{对话名}.md`）
- 消息按 `timestamp` **升序排列**。
- 每条消息格式：
  ```markdown
  **[发送者名]** | {格式化时间}
  > {消息文本}
  ```
- **卡片链接处理**：
  - 若消息中包含形如 `"scheme": "llspace://card/...` 的字段，且 `text` 中含 `<a>...</a>` 链接：
    - 调用 `API_specs.md` 中 **“获取卡片详情”** 接口，传入卡片 ID。
    - 从响应中提取卡片的 `url`。
    - 下载该 `url` 的完整网页快照（含 CSS、图片、JS），保存至 `{对话目录}/cards/{card_id}.html`，并本地化资源。
    - 在 Markdown 中将原文 `<a>love yourself</a>` **替换为** `[卡片: love yourself]({相对路径}/cards/{card_id}.html)`。

##### (b) HTML 文件（`{对话名}.html`）
- 整体背景色：`#181818`
- 消息气泡样式：
  - **我方消息**：
    - 背景色：`#FFCA01`
    - 字体颜色：`#000000`
    - 右对齐
  - **对方消息**：
    - 背景色：`#242424`
    - 字体颜色：`#FFFFFF`
    - 左对齐
- 每条消息包含：消息气泡上方的格式化时间、气泡内的消息内容。
- 模拟微信式气泡布局，好友名称显示在页面最上方。
- **卡片链接同样替换为本地 HTML 快照路径**，并可点击跳转。

##### (c) JSON 文件（`{对话名}.json`）
- 包含完整元数据与消息列表。

---

### 通用要求
- **API 依赖**：所有接口调用（会话列表、会话内容、卡片详情）**必须严格依据 `API_specs.md`**。若文档未明确定义某字段或参数，**不得猜测**，应跳过相关处理并记录警告日志。
- **进度反馈**：导出过程中显示进度条，包括当前对话、已处理消息数、总体百分比。
- **错误处理**：单个对话导出失败不应中断整体流程，应记录错误并继续处理其余对话。
- **线程安全**：所有网络请求和文件操作必须在后台线程执行，GUI 保持响应。
- **返回主页**：导出完成后自动返回主页，并显示摘要（导出对话数、消息总数、保存路径）。
- 不修改原有卡包导出逻辑，确保功能并存。
